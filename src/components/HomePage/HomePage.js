import "./HomePage.css";

import React from "react";

const HomePage = () => {
  // API's
  const postsApi = "https://jsonplaceholder.typicode.com/posts";
  const photosApi = "https://jsonplaceholder.typicode.com/photos";

  const somePosts = []
  const somePhotos = []
  return (
      const getSomePhotos = useEffect(() => {
          const result = fetch(photosApi).then(res => res.json())
      })
  <div>HomePage</div>
  );
};

export default HomePage;
